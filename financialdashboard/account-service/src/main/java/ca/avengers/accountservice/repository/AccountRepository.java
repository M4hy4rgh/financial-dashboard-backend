package ca.avengers.accountservice.repository;

/**
 * This will be implemented in the future.

 * This interface is used to interact with the database and perform CRUD operations on the Account entity.
 * It extends the JpaRepository interface provided by Spring Data JPA.
 * JpaRepository provides methods for performing CRUD operations on the entity.
 * The Account entity is the model class that represents the account data in the database.
 * The Account entity is annotated with the @Entity annotation to indicate that it is a JPA entity.
 * The @Table annotation is used to specify the name of the table in the database where the account data is stored.
 * The AccountRepository interface extends the JpaRepository interface and provides additional methods for querying the account data.
 *
 */
public class AccountRepository {
}
